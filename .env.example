# Copy to .env and adjust. Loaded by src/medrag/config.py (prefix MEDRAG_).

# Postgres connection (local trust auth as the `luke` superuser by default).
MEDRAG_DATABASE_URL=postgresql://luke@localhost:5432/medrag

# Compute device for embeddings / reranker / LLM: auto | cpu | cuda
MEDRAG_DEVICE=auto
